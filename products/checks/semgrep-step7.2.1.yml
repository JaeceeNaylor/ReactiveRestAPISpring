rules:
  - id: step7.2.1-observe-getall
    languages: [java]
    severity: ERROR
    message: 'Step 7.2 incomplete: Add @Observed(name = "products.http.getAll") above getAll.'
    paths:
      include:
        - src/main/java/com/example/products/api/ProductController.java
    pattern: '@Observed(name = "products.http.getAll")'
