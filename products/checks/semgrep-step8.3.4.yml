rules:
  - id: step8.3.4-getendpoint
    languages: [java]
    severity: ERROR
    message: "Step 8.3 incomplete: Add a GET endpoint returning Flux<Product>."
    paths:
      include:
        - src/main/java/com/example/products/api/ProductControllerV2.java
    pattern: |
      public Flux<Product> getAll() {
        return repo.findAll();
      }
