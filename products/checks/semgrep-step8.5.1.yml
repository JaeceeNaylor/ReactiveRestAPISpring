rules:
  - id: step8.5.1-missing-get-price-getter
    languages: [java]
    severity: ERROR
    message: "Step 8.5.1 incomplete: Add public BigDecimal getPrice() that returns price."
    paths:
      include:
        - src/main/java/com/example/products/domain/Product.java
    patterns:
      - pattern: |
          public BigDecimal getPrice() { return price; }
